import styled from "styled-components";
const Container = styled.div``;
export default function Wishlist() {
	return (
		<Container className="bg-lightgray">
			This is the wishlist box (work in progress...)
		</Container>
	);
}
