import styled from "styled-components";
const Container = styled.div`
	padding: 16px;
	width: 320px;
	border-radius: 14px;
`;
export default function Wishlist() {
	return (
		<Container className="shadow bg-lightgray">
			Wishlist box (work in progress...)
		</Container>
	);
}
